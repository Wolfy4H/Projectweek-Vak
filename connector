<?php

$servername = "localhost"
$username = "admin_sd"
$password = "123"
$db = "ProjectVak"

$dns = "mysql:host=$servername;dbname=$db;charset=";

try {
    $pdo = new PDO($sdn, $username, $password);
} catch (\PDOException $e) {
    throw new \PDOException($e->getMessage(), (int)$e->getCode());
}

$name = $_POST [''];